# blk93@0x1c18a6 — bounded RAM-word forwarder **[V][D][O]**

- **Bounds**: entry `0x1c18a6`, exit `0x1c1f7d` (exclusive), 654 instructions
  (`entry=0x1c18a6 exit=0x1c1f7d n_insns=654`).

## Verified bounded interval

In both DT2 1.15C and DT2 1.16, the identical raw instructions are:

- `0x1c18f3`, `100400254d9c`: Type14a `R4 = DM(0x254d9c)`.
- `0x1c1928`, `110400254d98`: Type14a `DM(0x254d98) = R4`.

The deterministic listing finds no intervening destination write to `R4`, so the
loaded value is forwarded unchanged on this straight-line bounded interval.
This does not establish global register immutability or natural execution.
**[V]**

The automated Ghidra proof gives the same path within `FUN_001c18a6` as
`ram 0x4a9b38 -> value -> ram 0x4a9b30`; address-space-aware exact matching
and raw p-code agree. It is an exact match in both DT2 images. **[V]**

## Cross-image evidence

Ignored deterministic evidence is
`out/ghidraq-dataflow/batch/combined/`: `machine-map.json` SHA-256
`8a02f7a4e8e822bbc395783c8649b20a7d6ef49c60c0660b2883fa58e2611964` and
`machine-map.sqlite` SHA-256
`f1002b2b9fb0012f37648ee65a7e97bdd5350b41fae9e6c88ade6ef68b600929`.
Its batch has 3 images, 24 queries, 72 results, 2 exact matches, and 0 errors.
The source blobs are DN2 1.11
`336e340aa0cdcd34e314cfa44849f709a3134f6bd4cd57dfc7e15702c83115e2`,
DT2 1.15C `6d4316cddd41edef7a136c10d270313882028a59b96cc8fe97a716b949a7d551`,
and DT2 1.16
`0f514a12a2255f5c081e292c47f1f29462003177658da4bbae0a22fd737fffa2`.
**[V]**

## Open

The runtime meaning, contents, and ownership of `0x254d98`/`0x254d9c`; their
relation to machine selection; the writer of `0x254d9c`; and natural branch
execution remain **[D][O]**. DN2's query status is `ok` with zero exact match,
but that is not a raw whole-image negative or an architectural-absence/analogy
claim. Likewise, zero exact matches for external-table addresses
`0x8055c840`, `0x8055c858`, and `0x8055c874` are a HighFunction/query
limitation. I6+124 identity remains **[D][O]**.

Next automated seeds: exhaustively scan Type14a raw instructions around
`0x254dxx` across DN2; recover the `0x254d9c` writer chain; slice callers and
control predicates into `0x1c18a6`; and obtain raw p-code/caller slices for
I6+124 and the table readers. **[O]**
